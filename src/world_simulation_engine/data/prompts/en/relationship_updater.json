[
  {
    "role": "system",
    "content": "You propose at most two small relationship changes for exactly one character perspective. Use only the supplied new memories, candidates, and existing relationships. Every change must cite at least one supplied new memory ID. Prefer no change unless the memory clearly changes or establishes a durable attitude, habit, goal, spatial fact, or compatibility belief. Deltas must be small. Never create objective/global truth, change another character's private view, invent endpoints, or repeat an unchanged relationship. Return only RelationshipUpdateProposal JSON with changes and updater_notes."
  },
  {
    "role": "user",
    "content": "Actor: {{ actor.name }} ({{ actor.id }})\nSimulation time: {{ simulation_time }}\nTurn: {{ turn_id }}\n\nNew memories:\n{% for memory in new_memories %}- {{ memory.id }}: {{ memory.summary }}\n{% else %}- none\n{% endfor %}\n\nAllowed entity endpoints:\n{% for entity in candidate_entities %}- {{ entity.type }} {{ entity.name or entity.id }} ({{ entity.id }})\n{% else %}- none\n{% endfor %}\n\nExisting relationships visible to this actor:\n{% for relationship in existing_relationships %}- id {{ relationship.id }}; {{ relationship.source.name or relationship.source.id }} ({{ relationship.source.id }}) --{{ relationship.label }}--> {{ relationship.target.name or relationship.target.id }} ({{ relationship.target.id }}); kind {{ relationship.details.kind }}; confidence {{ relationship.confidence }}; details {{ relationship.details }}; private description {{ relationship.private_description }}; evidence {{ relationship.evidence_memory_ids }}\n{% else %}- none\n{% endfor %}\n\nFor updates, supply relationship_id and the same endpoints/kind. For new records, omit relationship_id. kind is one of interpersonal, spatial, interaction, goal, compatibility, generic. Fill only fields relevant to that kind."
  }
]
